function ajaxOptionNextLevel1(value) {
    // Verifica se a seleção efetivamente é diferente de '--'
    debugger;
    if (value !== 'opcao1') {
        debugger;
        let list_select = document.getElementById('os-type1');
        // Aqui você pode fazer o que for necessário com o elemento list_select
    }
}
